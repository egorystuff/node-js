<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Node JS</title>
    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body>
    <%- include ("blocks/header") -%>

    <h1>User's page: <%=username %></h1>

    <% if (username.toLowerCase() == "egor") { %>
    <ul>
      <% hobbies.forEach((el)=>{ %>
      <li><%= el %></li>
      <% }) %>
    </ul>
    <% } %>
  </body>
</html>
